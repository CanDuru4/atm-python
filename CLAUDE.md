@AGENTS.md

## Claude Code
- Flask is not installed globally; create the venv from AGENTS.md before a live `app.py` run. `py_compile` needs no install.
- No plan mode or subagents needed; the repo is two Python files plus manifests.
